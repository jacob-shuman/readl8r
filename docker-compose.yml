services:
  readl8r:
    image: jacobshuman/readl8r:latest
    container_name: readl8r
    restart: unless-stopped
    volumes:
      - ./data:/data
    environment:
      SECURE: false
    ports:
      - 8080:80
